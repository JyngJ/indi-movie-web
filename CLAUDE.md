@AGENTS.md

Follow the branching and architecture rules defined in `AGENTS.md` for all work: create dedicated branches from `main`, use typed branch prefixes such as `feature/`, `fix/`, and `docs/`, apply Clean Architecture boundaries, and never merge into `main` without explicit user approval.
